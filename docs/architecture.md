# Architecture

The project uses three layers:
- Agent Registry layer.
- Evaluation layer.
- Governance layer.

The registry stores identity, purpose, tools, and risk level.
The evaluation layer measures performance and behavior.
The governance layer checks compliance, authorization, and auditability.
